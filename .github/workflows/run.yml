name: Pipeline

on:
  push:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  lint-fix:
    uses: ./.github/workflows/lint-fix.yml

  lint-check:
    needs: run-fix
    uses: ./.github/workflows/lint-check.yml
